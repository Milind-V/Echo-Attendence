import { ME_STUDENT, ME_TEACHER, GOOGLE_AUTH, LOGOUT } from "./UserQueries";
import {
	CREATE_CLASS,
	CLASS,
	TAKE_ATTENDENCE,
	JOIN_CLASS,
	MARK_ATTENDENCE,
} from "./ClassQueries";

export {
	ME_STUDENT,
	ME_TEACHER,
	GOOGLE_AUTH,
	LOGOUT,
	CREATE_CLASS,
	CLASS,
	TAKE_ATTENDENCE,
	JOIN_CLASS,
	MARK_ATTENDENCE,
};
